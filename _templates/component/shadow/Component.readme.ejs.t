---
to: "<%= h.src() %>/components/<%= name %>/Readme.md"
---
### <%= name %> (variant:'default')

<%= name %> extends the [<%= name %>](https://theme-ui.com/components/<%= h.changeCase.lower(name) %>) provided by Theme UI

```js
  <<%= name %> />
```

### Variants of <%= name %>

```js
  <<%= name %> variant="default" />
  <<%= name %> variant="primary" />
  <<%= name %> variant="secondary" />
  <<%= name %> variant="outline" />
  <<%= name %> variant="accent" />
  <<%= name %> variant="muted" />
```
